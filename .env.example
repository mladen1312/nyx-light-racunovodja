# ════════════════════════════════════════════════════════════════
# Nyx Light — Računovođa — Konfiguracija okoliša
# 
# Kopirajte u .env i prilagodite:  cp .env.example .env
# ════════════════════════════════════════════════════════════════

# ── Server ──
NYX_HOST=0.0.0.0
NYX_PORT=7860
NYX_WORKERS=4

# ── LLM (vllm-mlx) ──
NYX_LLM_PORT=8080
NYX_VLM_PORT=8081
NYX_LLM_MODEL=mlx-community/Qwen3-235B-A22B-4bit
NYX_VLM_MODEL=mlx-community/Qwen3-VL-8B-4bit

# ── Autentikacija ──
NYX_JWT_SECRET=promijeni-ovo-u-random-string-64-znaka
NYX_ADMIN_PASSWORD=admin123
NYX_SESSION_TIMEOUT_MINUTES=480

# ── Putanje ──
NYX_DATA_DIR=./data
NYX_BACKUP_DIR=./data/backups
NYX_LAWS_DIR=./data/laws
NYX_UPLOAD_DIR=./data/uploads

# ── RAG ──
NYX_RAG_ENABLED=true
NYX_QDRANT_PATH=./data/qdrant
NYX_NEO4J_URI=bolt://localhost:7687
NYX_NEO4J_USER=neo4j
NYX_NEO4J_PASS=password

# ── Memory ──
NYX_MEMORY_L2_PATH=./data/db/semantic.db
NYX_DPO_ENABLED=true
NYX_DPO_SCHEDULE=02:00

# ── Mreža ──
NYX_MDNS_ENABLED=true
NYX_TAILSCALE_ENABLED=false
NYX_ALLOWED_ORIGINS=http://localhost:7860,http://nyx-light.local:7860

# ── Logiranje ──
NYX_LOG_LEVEL=INFO
NYX_LOG_FILE=./data/nyx.log

# ── ERP Integracija ──
NYX_ERP_DEFAULT=cpp
# cpp | synesis
